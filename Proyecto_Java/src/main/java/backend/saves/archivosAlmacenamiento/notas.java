package backend.saves.archivosAlmacenamiento;

public @interface notas
{
    // Esta clase no se utiliza en el proyecto
    // Es solo un archivo de notas
    // para que el metodo main pueda detectar el paquete

    // Adicional: Es necesario que los archivos contengan al menos una instancia de cada clase
    // de lo contrario la aplicacion no puede ejecutarse correctamente
    // (Se llega al EOF al intentar leer un objeto que no existe en el archivo)
}
